# Подтверждённые проблемы и краткий план исправлений

Дата проверки: 17 июля 2026 года. Проверены отчёты из этого каталога, код
базового коммита `010bc8329a1e111f255f23e7f45ef08ff5b329f8` и рабочее дерево перед
выпуском 2.1.4. Повторяющиеся находки объединены.

## Подтверждённые проблемы

| Приоритет | Проблема | Краткое решение | Статус в 2.1.4 |
|---|---|---|---|
| Критический | Переименование таблиц `sib_kr_*` в `dr_kr_*` без миграции теряло доступ к существующим ACL | Идемпотентно перенести данные в транзакции, старые таблицы не удалять | Исправлено |
| Высокий | Перемещение разделов проверяло не все исходные и целевые узлы | Независимо требовать право записи на объект и обоих родителей | Исправлено |
| Высокий | Полное дерево, владельцы и ACL выдавались любому авторизованному пользователю | Фильтровать дерево и записи на сервере; чужой субъект — только администратору | Исправлено |
| Высокий | Установщик зависел от jQuery: ранее загружал 1.7.2 с CDN, а локальная 3.7.1 из защищённого `/bitrix/modules` возвращала 403, оставляя кнопку установки выключенной | Использовать нативный JavaScript без внешней или локальной web-загрузки библиотеки | Исправлено |
| Средний | При новой установке не было безопасной автоматической генерации клиентской ключ-фразы | Добавить CSPRNG-генератор в форму и обязательный серверный fallback для пустого поля | Исправлено |
| Средний | Установщик начинал изменение системы без полной проверки доступности СУБД, модулей и целевых путей | Выполнять preflight с пробной записью в БД и проверкой PHP/Bitrix, криптографии, модулей, исходников и прав записи | Исправлено |
| Средний | Несуществующего или неактивного пользователя можно было назначить владельцем; `getUserById()` не обрабатывал отсутствие строки | Проверять результат выборки, ID и активность пользователя | Исправлено |
| Средний | Ключи хранились открыто рядом с данными, а клиентский ключ долго оставался в глобальном JS-объекте | Ключ сервера брать из окружения либо защищать ключом Bitrix; удалить клиентский ключ из глобального объекта после KDF | Исправлено; остаточный XSS-риск документирован |
| Средний | CSV-экспорт позволял формулы, а импорт не имел достаточных лимитов состояния | Нейтрализовать формульные префиксы; ограничить тело, строки, TTL и размер состояния | Исправлено |
| Средний | Промежуточное состояние импорта содержало пароли в открытом виде в PHP-сессии | Шифровать и аутентифицировать сериализованное состояние серверным ключом, привязать к пользователю и TTL | Исправлено |
| Средний | `getRequestParams()` смешивал JSON, GET, POST и Cookie через `$_REQUEST` | Разбирать валидный JSON по Content-Type, иначе брать только GET или POST по методу | Исправлено |
| Средний | Справочник сотрудников был доступен без права управления ACL | Проверять право управления ACL перед выдачей пользователей и отделов | Исправлено |
| Средний | Ошибки установки игнорировались, а ACL удалялся до подтверждённого удаления раздела | Проверять каждый шаг и очищать ACL только после успешного удаления объекта | Исправлено |
| Средний | Интеграционный тест мог завершиться успешно, ничего не проверив | Явно обозначить optional job и поддержать обязательный режим `KEYRIGHTS_TEST_REQUIRED=1` | Исправлено |
| Средний | Запрос доступа допускал спам и внедрение BBCode | Проверять текущий доступ, экранировать имя, добавить rate limit и проверку отправки | Исправлено |
| Средний | Некорректные, дублирующиеся и неоднозначные ACL сохранялись частично | Валидировать субъектов и сохранять ACL транзакционно | Исправлено |
| Низкий | Ошибки API часто возвращались с HTTP 200 и без защитных заголовков | Возвращать подходящие HTTP-коды, `nosniff`, запрет фреймов и referrer | Исправлено |
| Низкий | Временное право сравнивалось без полного календарного дня | Сравнивать дату окончания с концом указанного дня | Исправлено |
| Низкий | Устаревшие ресурсы, `node_modules`, демо-файлы и отладочные фрагменты попадали в модуль | Удалить неиспользуемое и исключить dev-каталоги из установки и архива | Исправлено |
| Низкий | Markdown-инструкции и лицензия могли попадать внутрь каталога модуля в релизном архиве | Класть документацию и LICENSE в корень архива рядом с `drdroid.keyrights/` и проверять структуру сборочным скриптом | Исправлено |
| Низкий | GitHub Actions ссылались на плавающие версии | Закрепить внешние actions по полным commit SHA | Исправлено |
| Низкий | Legacy-дешифратор содержал недоступный при PHP 8.2+ fallback на mcrypt | Оставить phpseclib/OpenSSL только для чтения старых данных | Исправлено |
| Низкий | Серверный ключ создавался через общий helper `randString()` | Использовать `random_bytes()` и hex-кодирование | Исправлено |
| Низкий | Сравнения ID владельца, пользователя и отдела допускали type juggling | Нормализовать ID к `int` и применять строгие сравнения | Исправлено |
| Низкий | Отдельный API endpoint менял глобальный `REQUEST_URI` для маршрутизации | Передавать нормализованный маршрут контроллеру явным параметром | Исправлено |

## Не подтверждено как уязвимость

- GET для read-only маршрутов не является обходом CSRF: write-маршруты уже
  требуют POST, авторизацию и корректный `csrf_token`; запрет всего GET сломал бы
  штатную загрузку списков.
- `filemtime()` с путём от `DOCUMENT_ROOT` сам по себе не раскрывает этот путь
  клиенту. Подавление ошибок всё же удалено в пользу `is_file()`.
- Ограниченный двухмегабайтным входом разбор legacy-свойства не демонстрирует
  заявленного ReDoS.
- Отсутствие CSP на JSON API и совместимость с IE11 являются настройками
  усиления/совместимости, а не доказанными эксплуатируемыми уязвимостями.

Полноценный пентест работающего портала и миграции на реальных MySQL/PostgreSQL
остаются отдельными задачами; статические и контрактные тесты их не заменяют.
